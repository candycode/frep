
const float s = 1.;

float $F( vec3 p )
{
  return p.x*p.x + p.y*p.y +p.z*p.z - s*(p.y - p.x*p.x )*(p.y - p.x*p.x );
}

vec3 $G( vec3 p )
{
  return 2.0 * vec3( p.x + 2. * s * p.x*p.y-2.*p.x*p.x*p.x, s * p.y*p.x*p.x, p.z );
}